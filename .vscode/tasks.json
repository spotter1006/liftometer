{
	"version": "2.0.0",
	"tasks": [
		{
			"type": "cppbuild",
			"label": "C/C++: g++ build",
			"command": "/usr/bin/g++",
			"args": [
				// "-Wl,--verbose ",
				"-O0",			// Optimization level 0
				"-pg",			// gprof profiling data
				"-I${workspaceFolder}../",
				"-pthread",
				"-fdiagnostics-color=always",
				"-g",
				"${workspaceFolder}/*.c*",
				"-o",
				"${fileDirname}/build/debug/liftometer",
				"-lgpiodcxx"		// This has to be after the object file creation step or symbols are not found
			],
			"options": {
				"cwd": "${fileDirname}"
			},
			"problemMatcher": [
				"$gcc"
			],
			"group": {
				"kind": "build",
				"isDefault": true
			},
			"detail": "compiler: /usr/bin/g++-9"
		}
	]
}